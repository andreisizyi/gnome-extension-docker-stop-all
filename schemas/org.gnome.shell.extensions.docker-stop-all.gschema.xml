<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.docker-stop-all"
          path="/org/gnome/shell/extensions/docker-stop-all/">
    <key name="docker-command" type="s">
      <default>''</default>
      <summary>Docker command or path</summary>
      <description>
        Optional Docker executable override. Supports command name from PATH
        (for example "docker") or absolute path (for example "/usr/bin/docker").
        Empty value means automatic discovery in PATH.
      </description>
    </key>
  </schema>
</schemalist>
